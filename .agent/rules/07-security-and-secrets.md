# Rule: Security & Secrets

Không ghi secret, token, cookie, bearer header, captcha hoặc session state vào source, `.agent`, `.manager`, `.feedback`, log hay report. Khi phát hiện phải báo PM và đề xuất rotate/cleanup.

